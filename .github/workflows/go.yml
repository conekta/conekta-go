name: Golang CI
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
jobs:
  tests:
    strategy:
      matrix:
        sdk: [1.17, 1.18, 1.19, 1.20.x, 1.21.x , 1.22.x, 1.23.x, 1.24.x, 1.25.x]
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v6
        with:
          go-version: ${{ matrix.sdk }}
      - name: Run Mockoon CLI
        uses: mockoon/cli-action@v3
        with:
          version: 'latest'
          data-file: 'https://raw.githubusercontent.com/conekta/openapi/main/mocks/conekta_api.json'
          port: 3000
      - name: Run tests
        run: |
          go test -v --race ./...
        env:
          BASE_PATH: localhost:3000
